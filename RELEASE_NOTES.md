# 星野 Starlight 0.2.2 — 固定签名基线版

免费、无广告、无订阅；Android 8.0+。

- 从本版开始使用固定发布签名；安装 0.2.2 后，后续更高版本可以直接覆盖升级，不再随每次构建更换签名。
- 延续 0.2.1 的方向修正：按当前屏幕方向重映射传感器矩阵，再从后置摄像头光轴与屏幕天顶计算方位、高度和画面旋转。
- 方向校准现在同时用于普通“指向识星”和相机 AR。
- 新增东、南、西、北一键校正和“旋转 180°”，可修正不同机型或磁场环境中的固定方向偏差。
- 方向/高度偏移范围扩大；AR 继续提供视场比例校准。
- 传感器精度较低时在界面提示校准，提醒远离磁铁和金属并缓慢画 8 字。

## 使用方法

先设置实际位置，再点击右侧准星。若方向不准，点击右侧齿轮，把手机背面朝向一个已知方位，然后点“当前设为北/东/南/西”。若恰好相差 180°，可直接点“方向正好相反”。

## 首次切换固定签名

0.1.0～0.2.1 使用过临时开发签名，因此无法通过新签名追溯覆盖。若系统提示签名冲突，请先在“观测收藏”导出 JSON 备份，卸载旧版，再安装 0.2.2 并导入备份。此操作只需一次；从 0.2.2 开始后续版本可以覆盖安装。

## 验证与限制

自动测试覆盖天文投影、方向校准状态、传感器低精度提示、备份和主要界面流程；原生代码编译与 APK 构建由 CI 验证。指南针仍会受到磁性手机壳、车辆、钢结构和设备传感器质量影响。本预览版尚未完成多机型真机方向验收。

相机 AR 是方向传感器叠加，不是图像识别或空间追踪。仍不含卫星/ISS、十亿星库、AI、天气/光污染、空间音频、多人同步和行星 AR 模型；不是 Night Sky 全功能等价版本。
