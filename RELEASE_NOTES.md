# 星野 Starlight 0.2.0 — 相机 AR 与观测备份

免费、无广告、无订阅；Android 8.0+。

- 新增实验性相机 AR：后置相机实景叠加星图，权限按需申请，退出/后台停止相机。只预览，不录音、不拍照、不上传。
- 新增 AR 左右/上下/视场手动校准；进入 AR 自动使用当前时间，禁止误拖动星图和误切换时间。
- 新增收藏笔记 JSON 导出/导入，使用系统文件选择器；导入不覆盖已有本机记录，并检查格式、记录长度和文件大小。
- 修复平板自然横屏方向下的传感器画面旋转。

## 验证与限制

自动测试覆盖天文投影、备份校验/合并、AR UI 状态和相机失败回退；原生代码编译与 APK 构建由 CI 验证。尚未完成 Android 真机摄像头、指南针对齐、文件选择器与耗电测试。AR 是方向传感器叠加，不是图像识别或空间追踪。

仍不含卫星/ISS、十亿星库、AI、天气/光污染、空间音频、多人同步和行星 AR 模型。不是 Night Sky 全功能等价版本。

## 安装注意

此为开发签名预览包，与 0.1.0 的签名可能不同。若无法覆盖安装，先保存旧版重要笔记再卸载旧版。0.1.0 无导出功能，需要手动复制；从 0.2.0 起可以导出 JSON 备份。
